# BMI-calculator-
This project is a single-page Body Mass Index (BMI) calculator built with vanilla HTML, CSS, and JavaScript — no frameworks, no build tools, no external dependencies beyond web fonts. It was built to practice core front-end fundamentals: form handling, numeric calculation, input validation, and conditional rendering based on that calculation.
